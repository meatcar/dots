# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

#set $mod Mod1
set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
#font pango:Terminus 5
font pango:Iosevka Medium 12
#font pango:Fantasque Sans Mono 18
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10

set $border 4

#new_window normal $border px
default_border pixel $border
default_floating_border normal $border px

gaps inner 30
gaps outer 10

smart_borders on
smart_gaps on

hide_edge_borders smart

for_window [class=".*"] title_format "<span font='Iosevka 9'>%class: %title</span>"
for_window [class="Google-chrome"] border pixel $border px
for_window [class="Chromium"] border pixel $border px
for_window [class="Firefox"] border pixel $border px
for_window [class="Code"] border pixel $border px
for_window [class="Opera"] border pixel $border px
for_window [class="Alacritty"] border none

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec term
bindsym $mod+Shift+Return exec cwd-term

# kill focused window
bindsym $mod+d kill

# start dmenu (a program launcher)
bindsym $mod+p exec --no-startup-id "zsh -c 'rofi -show combi -combi-modi window,run,drun,ssh -sidebar-mode -lines 0 -modi combi,window,run,drun,ssh'"
# bindsym $mod++p exec --no-startup-id albert show
bindsym $mod+Shift+p exec --no-startup-id zsh -c 'rofi-pass'
bindsym $mod+e exec pcmanfm
bindsym $mod+Shift+e exec --no-startup-id rofi -show emoji
bindsym $mod+g exec gvim
bindsym $mod+Shift+c exec emacsclient -nc

# turn of ctrlq in firefox
bindsym Ctrl+q exec --no-startup-id noctrlq.sh

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

bindsym $mod+u [urgent=latest] focus

# split in horizontal orientation
bindsym $mod+Shift+v split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+t layout tabbed
bindsym $mod+s layout toggle split
bindsym $mod+Shift+t layout stacking

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

bindsym $mod+grave fullscreen disable, \
          floating enable, \
          border pixel $border, \
          resize set 400 300, \
          sticky enable, \
          move window to position 0 0
bindsym $mod+y exec xclip -o | xargs mpv
bindsym $mod+x sticky toggle

# show/hide scratchpad
bindsym $mod+z scratchpad show
# move window to scratchpad
bindsym $mod+Shift+z move scratchpad

# conky
for_window [instance="Conky"] move scratchpad, \
                              scratchpad show
bindsym $mod+c [instance="Conky"] scratchpad show


# switch to workspace
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10

bindsym $mod+Shift+r exec rename-workspace

assign [class="(?i)emacs" title="^(?!capture)"]  2
assign [class="(?i)google-chrome"]               1
assign [class="(?i)firefox"]                     1
#assign [class="(?i)chromium"]                    1

no_focus   [window_role="pop-up"]
for_window [window_role="pop-up"] floating enable
for_window [class="Gloobus-preview"] floating enable, \
                              border pixel 1, \
                              move window to position center
for_window [class="Nemo-preview-start"] floating enable, \
                              border pixel 1, \
                              move window to position center
                              #move position center
for_window [class="Keybase" instance="keybase"] floating enable, \
                              move window to position mouse
for_window [class="dialog"] floating enable
for_window [class="(?i)emacs" title="^capture"] floating enable

# reload the configuration file
#bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+q restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+q exec --no-startup-id "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
bindsym $mod+r mode "resize"
mode "resize" {
  # These bindings trigger as soon as you enter the resize mode

  bindsym h resize shrink width 10 px or 10 ppt
  bindsym j resize grow height 10 px or 10 ppt
  bindsym k resize shrink height 10 px or 10 ppt
  bindsym l resize grow width 10 px or 10 ppt

  bindsym Shift+h resize shrink width 100 px or 100 ppt
  bindsym Shift+j resize grow height 100 px or 100 ppt
  bindsym Shift+k resize shrink height 100 px or 100 ppt
  bindsym Shift+l resize grow width 100 px or 100 ppt

  # back to normal: Enter or Escape
  bindsym Return mode "default"
  bindsym Escape mode "default"
}

# change gaps sizes
bindsym $mod+Shift+g mode "gaps"
mode "gaps" {
  bindsym k gaps inner all plus 1
  bindsym j gaps inner all minus 1

  # back to normal: Enter or Escape
  bindsym Return mode "default"
  bindsym Escape mode "default"
}

# move windows around
bindsym $mod+m mode "move" focus floating
mode "move" {
  bindsym $mod+Tab focus right

  bindsym h     move left
  bindsym j     move down
  bindsym k     move up
  bindsym l     move right

  bindsym Shift+h     move left 100
  bindsym Shift+j     move down 100
  bindsym Shift+k     move up 100
  bindsym Shift+l     move right 100

  # back to normal: Enter or Escape
  bindsym Return mode "default"
  bindsym Escape mode "default"
}

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
#bar {
  #i3bar_command bars
#}

# Set colors from Xresources as per
# https://github.com/dylanaraps/pywal/wiki/Customization#i3
set_from_resource $fg           XTerm.foreground  #3498DB
set_from_resource $bg           XTerm.background  #ffffff
set_from_resource $focus        color7      #34495E
set_from_resource $focusi       color8      #7F8C8D
set_from_resource $urgent       color10     #E74C3C

# class                 border  backgr. text indicator child_border
client.focused          $focus  $focus  $bg  $bg       $focus
client.focused_inactive $focusi $focusi $fg  $fg       $focusi
client.unfocused        $bg     $bg     $fg  $fg       $bg
client.urgent           $urgent $urgent $bg  $fg       $urgent
client.placeholder      $bg     $bg     $fg  $fg       $bg

client.background       $bg

## Autostart

exec_always --no-startup-id bars
exec_always --no-startup-id set-wal
exec --no-startup-id mydunst
exec --no-startup-id sleep 3 && dex -ae i3
