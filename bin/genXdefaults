#!/bin/bash

source ~/dots/colors/colors.sh
xrdb -merge "$COLORSCHEME_DIR/xresources/$COLORSCHEME"'.256.xresources' 

if [ $(hostname) == 'thumbnail' ]; then
    opts='-Ddpi120'
fi

xrdb -merge ~/.Xdefaults $opts 

