#!/bin/bash

source ~/dots/colors/colors.sh
xrdb -merge "$COLORSCHEME_DIR/xresources/$COLORSCHEME"'.xresources' 

if [ $(hostname) == 'thumbnail' ]; then
    opts='-Ddpi120'
fi

xrdb -merge ~/.Xdefaults $opts 

